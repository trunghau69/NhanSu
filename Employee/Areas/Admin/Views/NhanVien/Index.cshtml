@model IEnumerable<Employee.Models.NhanVien>
@{
    ViewBag.Title = "Danh Sách Nhân Viên";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="mb-3">
        <h1 class="page-header text-center text-uppercase">@ViewBag.Title</h1>
    </div>
</div>

<!-- Main content -->
<section class="content">
    <!-- Default box -->
    <div class="card shadow-lg rounded-lg border-0">
        <div class="card-header d-flex justify-content-between align-items-center bg-primary text-white rounded-top">
            <!-- Nút thêm mới -->
            <div class="d-flex justify-content-start">
                <a asp-area="Admin" asp-controller="NhanVien" asp-action="ThemMoi" class="btn btn-success btn-sm" style="padding: 5px 15px; font-size: 14px;">
                    <i class="fa fa-plus"></i> Thêm Mới
                </a>
            </div>
        </div>

        <div class="card-body p-0">
            <table class="table table-hover table-striped table-bordered mb-0">
                <thead class="table-light">
                    <tr>
                        <th class="text-center align-middle" style="width: 5%;">STT</th>
                        <th class="text-center align-middle" style="width: 15%;">Mã Nhân Viên</th>
                        <th class="text-center align-middle" style="width: 20%;">Tên Nhân Viên</th>
                        <th class="text-center align-middle" style="width: 15%;">Giới Tính</th>
                        <th class="text-center align-middle" style="width: 15%;">Điện Thoại</th>
                        <th class="text-center align-middle" style="width: 20%;">Chức Vụ</th>
                        <th class="text-center align-middle" style="width: 10%;">Hành Động</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        var i = 1;
                        foreach (var item in Model)
                        {
                            <tr>
                                <td class="text-center align-middle">@i</td>
                                <td class="text-center align-middle">@item.MaNhanVien</td>
                                <td class="text-center align-middle">@item.TenNhanVien</td>
                                <td class="text-center align-middle">@item.GioiTinh</td>
                                <td class="text-center align-middle">@item.SDT</td>
                                <td class="text-center align-middle">@item.ChucVu?.TenChucVu</td>
                                <td class="text-center align-middle">
                                    <a asp-area="Admin" asp-controller="NhanVien" asp-action="Sua" asp-route-id="@item.ID"
                                       class="btn btn-warning btn-sm me-1" data-toggle="tooltip" data-placement="top" title="Sửa">
                                        <i class="fa fa-pencil-square-o me-1" aria-hidden="true"></i>Sửa
                                    </a>
                                    <a asp-area="Admin" asp-controller="NhanVien" asp-action="Xoa" asp-route-id="@item.ID"
                                       class="btn btn-danger btn-sm" data-toggle="tooltip" data-placement="top" title="Xóa">
                                        <i class="fa fa-trash-o me-1" aria-hidden="true"></i>Xóa
                                    </a>
                                </td>
                            </tr>
                            i++;
                        }
                    }
                    else
                    {
                        <tr>
                            <td class="text-center" colspan="7">Không có bản ghi nào!</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>
